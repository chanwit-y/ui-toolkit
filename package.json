{
  "name": "gummy-ui",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/ui dev & bun run --cwd apps/example dev & wait",
    "build": "bun run --cwd packages/ui build && bun run --cwd apps/example build && bun run --cwd apps/studio build",
    "studio": "bun run --cwd apps/studio dev",
    "example": "bun run --cwd apps/example dev",
    "tauri:dev": "bun run --cwd apps/studio tauri:dev",
    "tauri:build": "bun run --cwd apps/studio tauri:build"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
