# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Gummy UI (`@gummy-ui/ui`) is a **config-driven React component library**. The headline feature is not the individual components but a declarative engine: you describe a CRUD screen with three plain-object configs (model + api + container) and the `Core` class renders a full data-table + modal-form UI with no JSX. See "The declarative engine" below — it is the single most important thing to understand here.

Bun-workspace monorepo. TypeScript strict, ESM-only, no CommonJS.

## Commands

Always prefix shell commands with `rtk` (60–90% token savings; see the RTK section in the global CLAUDE.md for the full reference). RTK passes through unchanged when it has no dedicated filter, so it is always safe.

```bash
rtk bun install                                   # install (postinstall dedupes esbuild)
rtk bun run dev                                    # full stack: builds ui, then runs ui --watch + api + example
rtk bun run example                                # example app only (Vite, port 3200)
rtk bun run api                                    # API server only (Hono, port 9000)
rtk bun run build                                  # build ui, api, example  (NOTE: also references apps/studio, which does not exist)
rtk bun run --filter @gummy-ui/ui build            # build the library only (tsup)
rtk bun run --filter @gummy-ui/ui typecheck        # tsc --noEmit — run before committing
rtk bun test packages/ui/src/model/master.test.ts  # tests use `bun:test`
```

`scripts/dev.mjs` is the dev orchestrator: it kills stale esbuild/vite processes and frees ports 9000/3200, runs the esbuild dedupe, builds `packages/ui` once, then spawns `ui` (watch) and `api`, and starts `example` after a 2s delay so it picks up the freshly built library.

**Build dependency order matters:** `apps/example` imports `@gummy-ui/ui` as `workspace:*` and consumes its built `dist/`. After changing library source, the library must be rebuilt (or running in `--watch`) before the example reflects it. Use `bun run dev` rather than starting apps individually.

### esbuild pinning (do not "fix" casually)
`esbuild` is pinned to `0.27.7` via a root `overrides`, and `postinstall` (`scripts/dedupe-esbuild.mjs`) deletes nested copies under `vite`/`tsup`. The example's `vite.config.mjs` also force-enables `supported.destructuring` because esbuild 0.27.7 otherwise errors lowering destructuring. Changing esbuild versions or removing these workarounds will break the build.

## Workspaces

- **packages/ui** — `@gummy-ui/ui`, the library. Built with **tsup** (ESM + `.d.ts`, `react`/`react-dom` external). CSS ships separately and must be imported by consumers as `@gummy-ui/ui/styles.css`.
- **apps/example** — Vite demo (port 3200). In dev, Vite **proxies** `/collection`, `/upload`, `/uploads`, `/health` to the API (`http://localhost:9000`); leave `VITE_API_URL` empty in dev. `vite.config.mjs` (not `.ts`) is the active config.
- **apps/api** — Hono backend (port 9000, `tsx` via nodemon). A mock CRUD + file-upload server backing the example (`countries`, `upload` routes; uploads written to `apps/api/uploads/`). Not part of the published library.

## The declarative engine (core architecture)

A screen is assembled from three configs, then handed to `Core`:

1. **model** (`TModelMaster`) — named data shapes (request/response/params/query). Plain objects of `"string" | "number" | "boolean" | "any"` and nested `{ type: "array", collection: {...} }`. Converted at runtime to TypeBox/Zod schemas via `packages/ui/src/model/` (`convertTModelToTypeBox`, `convertTModelToTArray`).
2. **api** (`TApiMaster<typeof model>`) — named endpoints. Each entry has `url`, `methods`, and string keys (`response`/`body`/`query`/`parameter`) that **reference model names**, giving end-to-end typing from endpoint to payload. URLs use `:param` placeholders.
3. **container** (`Container[]` of `Bin[]`) — responsive layout. Each `Bin` carries breakpoint spans (`sm/md/lg/xl` as 12-col strings), a `type` (`textfield`, `select`, `avatar`, `hidden`, `text`, …), grid alignment, and an `element` describing the field (`name`, `dataType`, `isRequired`, validation, etc.).

Wiring (see `apps/example/src/App.tsx` and `apps/example/src/config/country/`):

```ts
const http = new HttpClientFactory(import.meta.env.VITE_API_URL ?? "", async () => "", "1.0.0", 30000);
const ui = new Core(http, model, api, containers).run();   // returns React elements
```

`Core` (`components/core/core.ts`) builds an `ApiFactory` → `ApiMaster` (binds endpoints to typed callers using the model) and a `ContainerBuilder`, then `.run()` returns the rendered tree. Must be rendered inside `<DataProvider>` + `<ThemeProvider>` (theme config sets per-component colors, e.g. button/dataTable). When adding a feature like the `country` example, add a sibling folder under `apps/example/src/config/<feature>/` with `model.ts`, `api.ts`, `container.ts`.

The builder layer lives in `packages/ui/src/components/core/` — `containerBuilder.tsx`, `elementBuilder.tsx`, `containerGrid.ts`, plus per-component config builders (`dataTable.ts`, `textField.ts`, `schema.ts`, `expression.ts`, …). `schema.ts`/`expression.ts` drive runtime validation and conditional logic.

## Other library subsystems

- **API layer** (`src/api/`): `HttpClientFactory`/`HttpClientBase` wrap Axios (base URL, async token getter, version, timeout, interceptors). `ApiFactory` + `APIMaster.ts` turn the `model`+`api` config into typed request functions. `observable/` provides RxJS wrappers; `hooks/useObservableCleanup.ts` unsubscribes on unmount.
- **Auth** (`src/auth/azure/`): Azure AD via MSAL. `getAccessToken` is the token entry point and is typically passed as the `HttpClientFactory` token getter.
- **Context** (`src/components/context/`): `DataProvider`, `LoadingProvider`, `ThemeProvider` — wrap the app; `ThemeProvider` carries the per-component theme map.
- **Routing + shell** (`src/components/core/PageRouter.tsx`, `core/pages.tsx`, `AppShell.tsx`): `PageRouter` renders one `Core` per page of a keyed `TPageMaster` record inside the consumer's own `<BrowserRouter>` and provides the record through `PagesProvider`; `ButtonElement.navigate` (the `"Navigate"` action) and `DataTableElement.rowNavigate` are `NavigateTarget`s resolved through it (`useNavigateTo`, warn-and-no-op on bad config). `AppShell` wraps `PageRouter` with a top bar (`brand` node or the config `appBar: AppBarConfig` — title, `icon`/`logo` mark, `variant: "panel" | "accent"` (accent paints the bar in `--accent-9` with contrast text, re-scoped via `color`), `height`, `sidebarToggle: { hide, show }` glyphs for the collapse button (defaults chevrons; `IconData` gained `panelLeft`/`panelLeftClose`/`panelLeftOpen`); collapse toggle, `header` slot; `menuIcons={false}` hides every menu item's icon — the rail then shows label initials) and a sidebar built from a `TMenu` (page links keyed to pages with fixed-param active matching, groups, external links, dividers; icon rail remembered in localStorage; off-canvas drawer below `md`). `navigation="top"` renders the same menu as a horizontal strip in the top bar instead (groups become Radix dropdowns, no sidebar; the drawer still serves mobile). A breadcrumb strip under the top bar follows `PageElement.parent` (a page key; ancestor links fill their `:params` by name from the current route), labelled by `title` — which may be a `DataValue` (`state`/`url`/`value`, resolved via `usePageTitle`, also the `document.title`); `breadcrumbs={false}` hides the strip app-wide, `PageElement.breadcrumb: false` per page. Code above the routed element must use `matchPage(pages, pathname)` for route params (`useParams()` is empty there). Surfaces use Radix vars (`--color-panel-solid`, `--gray-*`, `--accent-*`) because the library's Tailwind has no `darkMode: 'class'`. See `apps/example/src/config/country/{pages,menu}.ts`.
- **Form list** (`formlist` Bin, `components/FormList.tsx`, builder `core/formList.ts`; grilled design, see memory `form-list-component`): a repeating form backed by CRUD APIs. `rowContainer` (fields only) renders once per row as its **own** react-hook-form instance (`ContainerGrid`, extracted from `containerBuilder.tsx`, draws the bins); the component owns the chrome — per row `[fields] [Save while dirty] [Remove]` and an Add button, both configurable: `addLabel/addIcon/addPosition (top|bottom)/addAlign (start|center|end)/addDisplay (both|icon|label)`, `removeLabel/removeIcon/removePosition (start|end|below — Save travels with Remove)/removeDisplay`, plus `saveLabel` and `emptyText`; icon-only buttons keep the label as aria-label + tooltip (`ChromeButton`). `apiCrud.read` is an `API` (DataValue `params/query/body` resolved via `resolveDataValues`, refetched on route/state change, held until every URL `:param` resolves); `create/update/delete` are `FormListMutationApiRef`s (`params` row-field map + `extraParams`/`extraBody`/`query` DataValue maps, snackbars, delete `confirmBox`); args follow the endpoint's declared (query, param, body) order read off `ApiMaster.apis`. Save creates (draft) or updates (by `idKey`), Remove confirms then deletes (drafts are discarded), every mutation refetches; refetch is registered under `name` for `reloadDataTable`. Drafts are local state rendered after the persisted rows. No `create` ⇒ no Add, no `delete` ⇒ no Remove, no `update` ⇒ persisted rows read-only. `schema.ts` skips `formlist` bins (the rows aren't fields of the enclosing form). Demos in `apps/example`: the **Form list** page (`/form-list`, `config/country/formListDemo.ts`) — a full-CRUD contacts list with a static-options select per row and a create-and-delete-only notes list that starts empty — over the mock API's `/collection/contacts` and `/collection/notes` (`apps/api/src/routes/formListDemo.ts`), plus the Languages list on the country detail page over `/collection/languages/:country[/:id]` (`:country` = id or code).
- **Repeater** (`repeater` Bin, `components/Repeater.tsx`, builder `core/repeater.ts`; grilled design, see memory `repeater-component`): the **read-only** sibling of the form list — `itemContainer` renders once per item of an array and that item is put in scope (`core/rowScope.tsx`: `RowScopeProvider` / `useRowScope` / `useDataValue`), so the components inside bind with `type:"row"` `DataValue`s: `typography`/`text` `value`, `avatar` `srcValue` (a URL string or a `{ src | url }` object) / `fallbackValue` — the static `text`/`src`/`fallback` is the fallback when the value resolves empty, and the builders mount the bound wrappers (`core/bound.tsx`) only when a binding is set. `{ type:"row", key:"none" }` is the item itself (primitive arrays); bindings read the **nearest** repeater only. Bin `condition`s see `ctx = { row, index }` (`{ key:"row", path:"featured" }`), and `useNavigateTo` merges the enclosing item as `row`, so a `Navigate` button inside an item just works; button **API** calls with row data are not supported yet. The array comes from exactly one source: `api` (the standard `API` shape — same plumbing as the form list's read, shared in `core/readApi.ts`: DataValue `params/query/body` resolved against URL + state + the *parent* item, held until every `:param` resolves, refetch registered under `name` for `reloadDataTable`) or `items: DataValue` (a `state` slice array, or a `row` field ⇒ **nested repeaters**). Layout: `itemSpan: { sm, md, lg, xl }` in the Bin's 12-col vocabulary (base `col-span-12` stacks items below `sm`) + `gap`; `itemSurface: none|outlined|elevation` + `itemPadding` draw the card with Radix vars; `itemNavigate: NavigateTarget` makes the whole item a link (role=link, Enter/Space; clicks on inner interactive elements are ignored). No paging in v1 (`emptyText`, loading line, error text). `schema.ts` skips `repeater` bins — inputs in the template are unsupported (a nested `container` bin mounts its own form/ctx, so its conditions don't see `row`, though bindings still do). Demo in `apps/example`: the **Repeater** page (`/repeater`, `config/country/repeaterDemo.ts`) — a country card grid over `countries` linking to the detail page, and regions over the mock API's `/collection/regions` (`apps/api/src/routes/regions.ts`) with a nested repeater over `row.countries`, a conditional "Featured" badge and a per-item Navigate button. Resolver tests: `core/dataValue.test.ts` (`bun:test`; test files are excluded from `tsc`).
- **Button variant**: `ButtonElement.variant` / `ButtonProps.variant` / `ButtonBaseProps.variant` is `ButtonVariant = "contained" | "outlined" | "text"` (default `contained`), mapped in `Button.tsx` (`RADIX_VARIANTS`) onto Radix `solid | outline | ghost`; `text` resets ghost's negative margins (`!m-0`) so the button keeps its box in a grid cell. `ButtonProps` omits Radix's own `variant` key (an intersection would collapse ours to `never`). The builder (`core/button.ts`) forwards it, so modal and popover trigger buttons get it too. Studio: `ButtonConfig.variant` (shared by button items and both trigger buttons; a Variant segmented control in `ButtonConfigPanel` and the two trigger sections of `ContainerHostConfigPanel`), exported only off the default, previewed through `ButtonBase`; workspace `version: 19` (`migrateV18` fills `contained`). Example: the country detail page's "Back to list" (`text`) / "Next country" (`outlined`) and the list's Filter trigger.
- **DataTable server pagination + action colours** (grilled design, see memory `datatable-pagination-theme-actions`): `DataTableElement.api.pagination` (`offsetKey`, `limitKey`, `searchKey?`, `totalPath`, `defaultPageSize?`, `pageSizeOptions?`) switches `DataTable2` to manual paging; `placement?: "query" | "body"` says where the keys go and is **inferred** when absent (the endpoint declares a `body` model ⇒ body, else query — so a GET pages through its query string). The builder passes the endpoint's declared segments (`resolveApiRef` in `core/readApi.ts`) as `apiSegments`, and the table calls the API with `callArgs` positional args so the DataValue `query`/`body` maps land in their declared slots (a hand-wired `api` without segments keeps the old merged single arg). The row actions in `DataTable2` and `DataTableEditable` are Radix `IconButton variant="soft" size="2"` (the same chrome as FormList's Save), so radius / hover / dark mode come from Radix: **edit** = `theme.components.dataTable.editButtonColor` → `button.color` → accent, **delete** = `deleteButtonColor` → red (the editable table's Save/Cancel are green/gray), and FormList's per-row Remove keeps `ghost` but takes the same `deleteButtonColor` → red — one key for every row-delete affordance. The other table roles (header, pagination, row hover) still use the legacy light-only static map when pinned. Mock API: `/collection/page` answers both `POST {offset,limit,search}` and `GET ?offset&limit&search`. Demo in `apps/example`: the **Pagination** page (`/pagination`, `config/country/paginationDemo.ts`) — three tables over the countries data: query placement over `countriesPagedGet` (inferred), body placement over `countriesPaged` (explicit `placement: "body"`), and the in-memory default with no `pagination` block; the library exports `DataTableElement` / `DataTableApi` / `DataTablePagination` / `DataTableColumnDef` for configs like it. Studio: `DataTableConfig.pagination` (`enabled`, `placement: 'auto'|'query'|'body'`, keys, `totalPath`, sizes) edited in the "Server pagination" section of `DataTableConfigPanel` — `FieldPicker`s over the endpoint's query/body model for the resolved placement (`segmentFields` / `paginationPlacement` in `rowFields.ts`) and the response model for the total, keys auto-seeded on endpoint pick (`seedPaginationKeys`); exported on `api.pagination` only while enabled, engine defaults left implicit. Workspace `version: 20` (`migrateV19`: pagination block on every table, the `countriesPagedGet` seed endpoint + `countryPageQuery` model, and the seeded Countries table moved onto it with pagination on — the query-placement demo; the example's Countries page keeps the POST/body one). On the Theme page the eight data-table colour roles are `ColorField`s (a swatch-dot trigger opening the `SwatchGrid` in the studio `Popover`, like `IconField`; the placeholder says what unset resolves to — "follows accent", "same as buttons" for edit, "default (red)" for delete). The studio also seeds a **Pagination** page (`seed-page-pagination`, `paginationDemoSeedItems` / `paginationDemoPage`: the same three tables as the example's page; workspace `version: 21`, `migrateV20` via `appendDemoPages`).
- **DataTable column pinning** (grilled design, see memory `datatable-column-pinning`): `ColumnDef.pin?: "left" | "right"` seeds TanStack's `columnPinning` (`DataTableProps.pinnedColumns`, built by the builder from the columns); the ACTION column is always pinned left; every other header carries a lucide `Pin`/`PinOff` toggle that pins/unpins **left** for the session (right is config-only, nothing persists). This needed a layout change shared with `DataTableEditable` (`styles/datatable.css`): the old block-table (`thead`/`tbody` as `display:block`, a 460px body scroller with `overflow-x:hidden`, rows `display:table`) could not host `position: sticky`, so both tables now use a standard table (`border-separate`) inside **one** `.datatable-scroll` (`overflow:auto; max-height:460px`) — `th` sticky `top:0`, pinned cells sticky at `column.getStart('left')` / `getAfter('right')` (`pinStyle` / `pinClass` in `DataTable2.tsx`), corner cells above both, pinned body cells paint the row surface and inherit the row hover, inner-edge shadow on the last-left / first-right pinned cell. Studio: `DataTableColumnConfig.pin: '' | 'left' | 'right'` (a Pin select in the column card's "More"), exported only when set, the canvas preview passes `pinnedColumns`; workspace `version: 22` (`migrateV21`: `pin: ''` on every table column + the seeded **Data table** page `seed-page-data-table`, `dataTableDemoSeedItems` / `dataTableDemoPage`). Demo in `apps/example`: the **Data table** page (`/data-table`, `config/country/dataTableDemo.ts`) — a half-width five-column countries table with edit/delete, `code` pinned left and `updated_by_name` pinned right.
- **DataTable server filter + sort** (grilled design, see memory `datatable-server-filter-sort`; `datatable` only): a table filters and sorts **through its API**. `DataTableElement.filterContainer` is a container of ordinary input bins the table renders as its own filter form (`components/DataTableFilter.tsx`: its own react-hook-form + the container's schema, drawn by the engine's `ContainerGrid` via the builder's `renderFilterBins`; deliberately **not** a `<form>` — a select's option buttons would submit it) with table-supplied **Apply / Clear**: Apply validates → `appliedFilters` → page 1 → refetch; nothing is sent before Apply. `filterDisplay: "popover"` (default — a header Filter button, `filterButton { label, icon, variant }`, badge = filters differing from their default) or `"inline"` (a bar above the table); `filterDefaults` are the opening filters (first fetch already filtered, Clear returns to them). Fields reach the request through a new `DataValue` `{ type: "filter", key: "<field>", path?, value? }` in any of `api.params` / `api.query` / `api.body`: blank (`""`/null/`[]`; `false`/`0` are kept) ⇒ the key is left out, `value` is the fallback (needed for a URL `:param`), and an unresolved `:param` holds the call (`urlParams` from the builder; the body shows "Choose the filters…" / "No rows match…"). For this `DataTable2` moved onto the full `resolveDataValues` (before, only `type:"value"` query/body entries were sent and `params` never) with `useRouteScope` (`core/readApi.ts` — router-optional `url` scope) + `useStateVersion`; the resolved request and the sort are part of the react-query key in **both** modes, so filters work with or without `api.pagination`. Sort: `api.sort { placement? (inferred like pagination), sortKey, orderKey, orderValues? { asc, desc }, default? { field, order } }` → `manualSorting`, single column, field = `ColumnDef.sortField` else the accessor (`sortFields` prop), page reset on change; the header sort icons show in server mode only when `api.sort` is set. Side fix: the library `Popover` now drops its `translate-*-0` classes once entered (`settled`) — a transformed box is the containing block of the selects' `position: fixed` dropdowns, which landed far off-screen inside any popover. Mock API: `/collection/page` (GET + POST) takes `name` (contains) / `code` (equals) / `updatedBy` (one-of: array, repeated key or comma list) / `sortBy` + `sortDir` (`asc|desc|1|-1`), no `limit` ⇒ all matches; `GET /collection/region/:region/page` (`:region` = region id or `all`); rows are spread over four editors. Tests: the `filter` cases in `core/dataValue.test.ts`. Studio: the data table's **second child canvas** is the filter form (`childCanvasCount` 2, `ensureDataTableCanvases`; "Show when" stays edit-canvas-only), `Layout/DataTableRequestPanel.tsx` adds three sections under the endpoint — **Server sort** (`DataTableSortConfig`), **Server filters** (`filtersEnabled`, Edit filter form drill-in, display, button, opening values) and **Request mapping** (`RequestMapRow { slot: params|query|body, key, source }`, source = `NavParamSource` or a **Filter field** with fallback; `:param` rows seeded on endpoint pick via `seedRequestParams`, keys picked from the endpoint's segment model) — plus a per-column **Sort field**; export via `requestMaps` / `dataTableSort` / `requestSourceValue` in `gridConfig.ts` (only when set; numeric / boolean / array text is parsed), the canvas shows the real Filter button / inline bar over a `ChildCanvasPreview`. Workspace `version: 30` (`migrateV29`: defaults + second canvas on every table, `regionCountriesPaged` + `regionPageParams` in the library, filter/sort fields on the seeded page models, the seeded **Server filter** page `seed-page-server-filter`). Demo in `apps/example`: the **Server filter** page (`/server-filter`, `config/country/serverFilterDemo.ts`) — query placement + default sort, body placement + inline + default filter + `orderValues` 1/-1, a filter in a URL `:param` with an `all` fallback, and a non-paginated table; the Countries page's formerly unwired Quick filter popover is now the `filterContainer` of its server-paged table.
- **DataTable filter via API** (grilled design, see memory `datatable-filter-api-demo`): a demo, not a feature — a `filterContainer` is an ordinary engine form, so an `autocomplete` / `multiAutocomplete` inside it takes the same `api` block as any form field, in the engine's three loading modes: **load once** (`api` with no `query`: one react-query fetch at mount, matched in memory), **search as you type** (`api.query` with a `{ type: "value" }` DataValue that receives the typed text, debounced) and **dependent** (`observeTo: "<parent>"` + `api.params: { x: { type: "observe" } }`, the parent `canObserve: true`: refetched per parent value, the child's value cleared, no options until the parent has one). Observe Subjects are keyed by field *name* app-wide, so a cascading pair needs names no other table shares. The picked value still reaches the request through `{ type: "filter", key }`; the client's query builder stringifies arrays as a comma list (`codes=TH,JP`), which the mock API's `list()` splits. Engine fixes that came out of it (`Autocomplete2.tsx`): the load-once query is keyed on the observed value and disabled while it is blank (a studio-authored cascade — `params` but no `query` — never refetched before), an empty parent clears the options, and the search-mode mount fetch no longer leaves "Loading…" up when the observed parent is blank; `Popover.tsx` portals its content into the enclosing `[role="dialog"|"alertdialog"]` when the trigger sits in one (`portalTargetFor`), else `<body>` as before — a Radix dialog sets `pointer-events: none` on `<body>` and its focus trap yanks focus back from anything outside its content, so a table's filter popover inside a modal (the studio Live Preview, any consumer modal) was drawn behind the dialog, then unclickable, then its text fields could not keep focus; `fixedOrigin` converts the viewport position into the coordinate space of a transformed modal box (the containing block of `position: fixed`), and the layer is `z-[100000] pointer-events-auto` like the autocomplete dropdown. Mock API: `codes` (comma list / repeated / array) on `/collection/page` GET + POST, plus `codes: "any"` on the example's page models. Demo in `apps/example`: the **Filter via API** page (`/filter-api`, `config/country/filterApiDemo.ts`) — Region over `regions` → `params.region` (fallback `all`); Countries multi over `searchCountries` → `query.codes`; Region (`canObserve`) → Country (`observeTo`, `regionCountriesPaged` with `params.region` observe + `query.name` search) → `params.region` + `query.code`. Studio: the seeded **Filter via API** page (`seed-page-filter-api`, `filterApiDemoSeedItems` / `filterApiDemoPage`; workspace `version: 31`, `migrateV30` via `appendDemoPages` + `codes` on the seeded page models — `pageFilterFields` now includes it) carries the load-once and dependent tables (source-mode autocompletes over `regions` / `regionCountriesPaged`, the child's Options → Observe; the parent is named `region` because the exported observe param key is the observed field's *name* and the URL param is `:region`); the search-as-you-type table isn't seedable (no typed-text query authoring). Side fix: `ObserveToField` (`SelectFieldConfigPanel.tsx`) lists the **active** canvas's items (`useActiveItems`) — it read the root canvas, so inside any child canvas (filter form, modal, tab) it showed "(missing item)".

- **DataTable column resize** (grilled design, see memory `datatable-column-resize`): both tables let the user drag a header's right edge — shared in `components/dataTableResize.tsx` (`useColumnResize`, `ColumnResizeHandle`, `sizeVars` / `sizeStyle` / `pinStyle`, `withFiller`). On by default; `DataTableElement.canResizeColumns` / `DataTableEditableElement.canResizeColumns` (`false` removes every handle), per column `ColumnDef.size` / `minSize` / `maxSize` (px; default min 60, no max — same keys on `DataTableEditableColumn`) and `enableResizing: false` to lock one; the ACTION column is always locked. The tables are `table-fixed` + `min-width:100%`, which stretches columns proportionally while they total less than the container, so there are two modes: **stretch** (the start — pixel-identical to before) and **exact** (each column its px size, a width-less `.datatable-filler-cell` per row taking the slack, placed before right-pinned columns). A table starts exact when any column declares a `size`; otherwise the first gesture `freeze`s it — rendered `th[data-column-id]` widths are measured (floored) into `columnSizing` under `flushSync`, *before* TanStack's resize handler reads its start size. TanStack `columnResizeMode: 'onChange'`; widths **and** the pinned sticky offsets reach the cells as CSS variables on the `<table>` (`--dt-col-<id>-size|left|right`), and `DataTable2` hands back the same memoised `<tbody>` element while `isResizingColumn`, so HTML cells don't re-sanitise per mouse move (the editable table re-renders normally). The handle is a focusable `role="separator"` (`.datatable-resize-handle`: invisible at rest, a `currentColor` line on hover / focus / drag): drag or touch resizes, double-click / Enter / Home resets to the measured (else config) width, ← → move 10px (Shift 50px). Session-only state, like pinning. `.datatable-body-cell` now transitions colours only — `transition: all` made pinned cells' sticky `left` trail the header. `IconData` gained `columns`. Studio: `ColumnSizingConfig` (`size|minSize|maxSize: number | ''`, `resizable`) on both tables' column configs, edited by the shared `Layout/ColumnSizeFields.tsx` (datatable: column card "More"; editable: the expanded card), `canResizeColumns` "Resizable columns" toggle on both panels, exported only off the defaults (`columnSizing` in `gridConfig.ts`), canvas previews pass the sizes (inert — drag in the Live Preview); workspace `version: 29` (`migrateV28`: defaults on every table/column + the seeded **Column resize** page `seed-page-column-resize`, `columnResizeDemoSeedItems` / `columnResizeDemoPage` — no editable table there, since the studio can't author its read `paths`). Demo in `apps/example`: the **Column resize** page (`/column-resize`, `config/country/columnResizeDemo.ts`) — default, configured + pinned + locked on a narrow cell, an editable table, and one with resizing off.
- **DataTable HTML columns** (grilled design, see memory `datatable-html-columns`): `ColumnDef.html?: string` renders a `datatable` cell from a config-authored template instead of the plain value (the editable table is out of scope). `util/template.ts` (`renderTemplate` / `templateFields` / `escapeHtml`, tests in `util/template.test.ts`): `{{path}}` placeholders are lodash `get` paths into the row, `{{value}}` is the column's own value after `useDateFormat`, every interpolated value is HTML-escaped, no logic or pipes. The result always passes `util/sanitizeHtml.ts` — **DOMPurify** (the library's only sanitiser; its own instance, empty output where no DOM exists) with a presentational profile: HTML only, the `<style>` tag and form controls forbidden (the `style` attribute stays), `target` allowed with `rel="noopener noreferrer"` forced on `_blank`, `data:` image sources only. `components/HtmlCell.tsx` mounts it (`.datatable-html-cell`) and, inside a router, turns plain same-origin `<a href="/…">` clicks into `navigate()` (modifier keys, `target`, `download`, other origins fall through; the row-click guard already ignores anchors). The builder sets column `meta.html`, which makes `renderCellWithHighlight` skip the search highlight; sorting / filtering / search keep using the accessor value, so a field shown *only* inside a template isn't client-searchable. Styling: `class` and `style` both survive — Tailwind classes only where the consumer's Tailwind scans the config file, so the portable path is `style` + Radix vars or the always-shipped theme-aware helpers in `styles/datatable.css` (`dt-strong`, `dt-muted`, `dt-badge`, `dt-badge-gray`, `dt-link`). Column ids are the accessors, so two HTML columns can't share one. Studio: `DataTableColumnConfig.html` (`''` = plain), edited by `Layout/ColumnHtmlField.tsx` in the column card's "More" — textarea, "Insert field" over the endpoint's response row (`rowModelFields`), snippets incl. one in-app link per page (`:param` → same-named `{{field}}`), and a one-row preview through the library's `renderHtmlCell` with a warning when the sanitiser removed something; exported only when set. The canvas renders the real `HtmlCell`, and the mock rows (`Layout/dataTablePreview.ts`, moved out of `GridItem`) also fill every field a template reads. Workspace `version: 25` (`migrateV24`: `html: ''` on every column + the HTML table appended to the seeded Data table page, `dataTableHtmlSeedItems`). Demos in `apps/example`: the second table of the **Data table** page, and the dedicated **HTML columns** page (`/html-columns`, `config/country/htmlColumnDemo.ts`) — five tables, one per aspect: the `dt-*` helpers + in-app / new-tab links, **rich cells** (a flex profile block whose flag is a `background:url({{avatar}})` so a non-URL avatar falls back to a gray placeholder, a `<dl>` of facts, a prose `<p>` with strong/em/mark/u, a native `<details>` disclosure, link pills), `<img>` + inline `style` with Radix vars, nested lodash paths over `regions` (`{{countries.length}}`, `{{countries.0.name}}`; empty when unresolved), and templates the sanitiser strips (script, `onclick`, `<style>`, form control, `javascript:` href). The studio seeds the same page (`seed-page-html-columns`, `htmlColumnDemoSeedItems` / `htmlColumnDemoPage`, links to `/countries/{{code}}`; workspace `version: 26`, `migrateV25` via `appendDemoPages`; `version: 27`, `migrateV26` inserts the rich-cells table `seed-item-html-columns-rich` into an existing page; `version: 28`, `migrateV27` refreshes that table's two first-draft templates when still unedited), and the canvas mock rows preview a `{{x.length}}` path as a count.
- **DataTable row header, merged cells, group headers** (grilled design, see memory `datatable-headers-merge`): four keys on a column. `ColumnDef.rowHeader` (both tables) — the row's label column: cells are `<th scope="row">` in a soft accent tint (`.datatable-row-header`, `--accent-2`/`--accent-12`, `--accent-3` on row hover), the column is pinned left right after ACTION (the `datatable` builder and the studio preview put it first in `pinnedColumns.left`); one per table. `ColumnDef.group` (both tables) — runs of adjacent columns with the same label become TanStack group columns (`groupColumns` in `components/dataTableLayout.tsx`, applied by both builders / `DataTableEditable`'s `enhancedColumns`), drawn as a `colSpan` band (`.datatable-group-header`, centred, no sort / pin / resize / filter chrome) in a header row above; a column outside any group draws once in the top row with `rowSpan` over every header row (`headerRows` walks TanStack's placeholder headers down to the leaf and dedupes), so no empty boxes; one level only; a group split by pinning becomes two bands (TanStack), and a group cell's sticky offset borrows its first leaf's (`headerPinStyle`). Every header cell is sticky, so the **second** header row must stick *below* the first, not at `top: 0` where it slid over the band on vertical scroll: `useHeaderRowTops` (a `ResizeObserver` per header `<tr>`) writes the measured row offsets onto the `<thead>` as `--dt-header-top-<n>` and `headerTopStyle(rowIndex)` sets each lower cell's `top` from it (both tables). `ColumnDef.mergeRows` / `mergeColumns` (`datatable` only) — `computeSpans(pageRows, visibleColumns)` on the rows on screen (the current page after sort / filter): rows first (consecutive rows with the same **raw** accessor value → one `rowSpan` cell, `.datatable-cell-merged` top-aligned, covered cells not drawn), then columns (adjacent flagged columns with the same value in a row → `colSpan`), never both on one cell; `null` / `undefined` / `""` never merge; a merged cell belongs to its first row (row hover / click). Both tables now carry a `<colgroup>` (`<col>` per visible leaf + filler) because `table-layout: fixed` reads the **first row's** cells for widths, which spanning header cells would confuse; spanned body cells drop their own `width`. `IconData` gained `table`. Mock API: `GET /collection/region-countries` (`apps/api/src/routes/regions.ts`) — one flat row per country with `region` (repeats down the rows) and `created_by_name` / `updated_by_name` (equal on most rows; every third row `System`). Demo in `apps/example`: the **Table layout** page (`/table-layout`, `config/country/tableLayoutDemo.ts`, `regionCountries` endpoint + `regionCountryRes` model) — everything at once (Region row header + `mergeRows`, People / Dates bands, `mergeColumns` editors, edit/delete), a sortable table showing merges re-form per page/sort, and an editable table with `rowHeader` + `group`. Studio: `DataTableColumnConfig.rowHeader / mergeRows / mergeColumns / group` and `DataTableEditableColumnConfig.rowHeader / group` — in the column card's "More": **Row header** (setting one clears the others, `setRowHeader`), **Merge equal rows / columns** (datatable only) and a **Group header** text; exported only when set; the canvas previews all of it through the real components (`groupColumns` from the library; mock rows repeat in pairs on a `mergeRows` column and hold `Shared N` on `mergeColumns` columns); workspace `version: 34` (`migrateV33`: defaults on every column of both table kinds, the `regionCountries` seed endpoint + model via `seedLibraryAdditions`, and the seeded **Table layout** page `seed-page-table-layout`, `tableLayoutDemoSeedItems` / `tableLayoutDemoPage` — the two data tables; the editable one isn't seeded, the studio can't author its read paths).

- **DataTable cell line clamp** (grilled design, see memory `datatable-cell-lines`; `datatable` only): a plain cell shows at most `DataTableElement.cellLines` lines (**default 2**, `0` = wrap freely as before) and a **cut** cell shows its whole text in a tooltip on hover; `ColumnDef.lines` overrides per column (`0` allowed). HTML columns (`meta.html`) and the ACTION column never clamp. `components/ClampedCell.tsx` (`ClampedCell`, `DEFAULT_CELL_LINES`, exported): the clamp is the `.datatable-clamp` rule (`display: -webkit-box; -webkit-box-orient: vertical; overflow: hidden`) plus the one inline `-webkit-line-clamp` (the config number); the tooltip is Radix `@radix-ui/react-tooltip` (`Tooltip.Provider` wraps `DataTable2`'s root; `.datatable-cell-tooltip` in `datatable.css`, `z-index: 100000` so it clears a modal, `--gray-12` panel like the AppShell rail), **controlled**: `onOpenChange` measures `scrollHeight > clientHeight + 1` and only then opens with the element's `textContent` (so a formatted date reads as displayed; no per-cell observers). Two traps: the clamp box must not be a flex item — the cell wrapper is `flex flex-col` and a flex item's `display: -webkit-box` is blockified to `flow-root`, so `ClampedCell` puts a plain `min-w-0` div between (also the tooltip trigger); and Chrome's standard `line-clamp` implementation *reports* `-webkit-box` as `flow-root` in `getComputedStyle` even when the clamp works — judge by `clientHeight`, not `display`. The builder (`core/dataTable.ts`) forwards `cellLines` and sets column `meta.lines` beside `meta.html`. Mock API: regions gained an `about` paragraph (`description` stays one line for the Repeater cards). Demo in `apps/example`: two regions tables on the **Data table** page (`/data-table`, `config/country/dataTableDemo.ts`) — the default with Description `lines: 1`, and `cellLines: 0` beside it. Studio: `CellLines` (`0…6`), `DataTableConfig.cellLines` (a "Cell lines" select in the table's Props, `DEFAULT_CELL_LINES` 2) and `DataTableColumnConfig.lines` (`''` = table setting; a "Lines" select in the column card's "More"), exported only off the defaults (`cellLines` when ≠ 2, `lines` when set), the canvas passes both to the real `DataTable2` and the mock rows (`dataTablePreview.ts`) give prose-like accessors (`description`, `about`, `note`, `summary`, `comment`, …) a paragraph so the clamp is visible; workspace `version: 32` (`migrateV31`: `cellLines: 2` on every table, `lines: ''` on every column, `about` on the seeded `regionRes` model, and the two regions tables `dataTableClampSeedItems` appended to the seeded **Data table** page, guarded on `DATA_TABLE_CLAMP_ITEM_ID`). The tooltip also has its own demo page in both apps: **Cell tooltip** (`/cell-tooltip`, `config/country/cellTooltipDemo.ts`; studio `seed-page-cell-tooltip`, `cellTooltipDemoSeedItems` / `cellTooltipDemoPage`, workspace `version: 33`, `migrateV32` via `appendDemoPages`) — four regions tables: the default clamp, `cellLines: 1`, `lines` per column (1 / 4), and `cellLines: 0` beside an HTML column, which never shows one.

- **Card** (`card` Bin, `components/Card.tsx`, builder `core/card.tsx`; grilled design after MUI's Card, see memory `card-component`): a `Paper` with MUI's slots in MUI's order — `header { title | titleValue, subheader | subheaderValue, avatar: AvatarElement, action: ButtonElement }` (avatar · title / subheader · action; `label: ""` + `icon` for the ⋮ icon button, default `variant: text`), `media { src | srcValue, alt, height (180) }` (an `<img>` band, `--gray-3` placeholder with an image glyph when empty or failed), `content: Container`, `actions: ButtonElement[]` + `actionsAlign: start | end` (a flex button row), `collapse: Container` + `collapseLabel` / `defaultExpanded` (a chevron `IconButton` at the row's end opens an animated `0fr → 1fr` grid section; `visibility` keeps a closed one out of the tab order), `navigate: NavigateTarget` (MUI `CardActionArea`: `role="link"`, Enter / Space, clicks on inner controls excluded via `CARD_INTERACTIVE_SELECTOR`), and Paper's `variant` / `elevation` / `square`. The static value of every text / image slot is the fallback when its `DataValue` binding resolves empty (`titleValue` / `subheaderValue` / `srcValue` — `{ type: "row" }` inside a repeater, `state` / `url` / `value` elsewhere), so a card inside a `repeater` is the headline case. The JSX parts are exported hook-free (`Card` with `onActivate`, `CardHeader`, `CardMedia`, `CardContent`, `CardActions`, `CardExpandButton`, `CardCollapse`); `CardView` assembles them from the slot props, resolves the bindings (`useDataValue`, so it needs a router like every bound element) and owns the expand state — the builder and the studio canvas both render it. `content` / `collapse` bins are drawn by `CardBins` on the **enclosing** form (a field in a card belongs to the page's form; `schema.ts` includes them like a tab's panels), with the bin `condition`s seeing the repeater item (`{ row, index }`) when there is one, else the container's `contextData`. The header avatar / action and the footer buttons go through their own element builders, so a header action can be a popover / modal trigger, and a card can itself be a `popover` **trigger** (`trigger.type: "card"`). Side fix: `colspan.css` gained `.xs-col-span-12` and the Repeater uses it as its base span — the `col-span-12` utility it used before is emitted *after* the partial's responsive rules in a consumer's Tailwind build and out-cascaded them, so `itemSpan` never applied (every item was full width). Mock API: countries rows gained `image` (a `w640` flag beside the `w40` `avatar`). Demo in `apps/example`: the **Card** page (`/card`, `config/country/cardDemo.ts`, menu icon `creditCard`) — MUI's basic / elevated cards, media (+ a missing image), the "complex interaction" card (avatar, ⋮, media, icon actions, expand) beside a card-as-popover-trigger, the action-area cards (one with an inner `ConfirmBox` button that must not navigate), and cards in a repeater over `countries`. Studio: `card` tile (Layout group), `CardConfig` (`header: CardHeaderConfig` with `titleBinding` / `subheaderBinding` / `avatarEnabled` + `avatar: AvatarConfig` / `actionEnabled` + `action`, `media: CardMediaConfig`, `actions: CardActionConfig[]` — a `ButtonItemConfig` + `id` — `actionsAlign`, `collapseEnabled` / `collapseLabel` / `defaultExpanded`, `navigate: StudioNavigate | null`, Paper fields), **two child canvases** (content, expanded section — the second exported only while enabled; `childCanvasCount` 2), `Layout/CardConfigPanel.tsx` (`ItemBindingField` per slot, a compact `CardActionFields` editor — label / icon / variant / Navigate — for the header action and the `SortableCardList` of footer buttons, `NavigateEditor` for the card click; the row source is offered inside a repeater's template), `cardElement` in `gridConfig.ts` (only off the defaults), `CardLivePreview` on the canvas through the real `CardView` (bindings as `{field}` tokens, always-live tier), `livePreview.ts` recurses into `content` / `collapse`; workspace `version: 35` (`migrateV34`: the `image` field on every seeded country row model + the seeded **Card** page `seed-page-card`, `cardDemoSeedItems` / `cardDemoPage`).
- **HTML content** (`html` Bin, `components/HtmlContent.tsx`, builder `core/htmlContent.ts`): a block of markup on the page — `HtmlContentElement { html, value?: DataValue, prose?: boolean }`. It is the data table's HTML-**column** pipeline over a bound value instead of a row: `html` is a `{{path}}` template (`util/template.ts`, every placeholder escaped, `{{value}}` = the bound value itself) rendered against `value` — `{ type: "row", key: "none" }` for the enclosing repeater item, a `state` slice, a URL param — or nothing (static markup; a placeholder then renders empty, so don't put literal `{{…}}` in static text); the result always passes `util/sanitizeHtml.ts` (DOMPurify presentational profile: no scripts / `<style>` / form controls, `class` + `style` + links kept, `javascript:` hrefs dropped). `HtmlContent` = `useDataValue(value)` + the existing `HtmlCell` (which gained a `className` prop), so same-origin `<a href="/…">` clicks navigate in-app and the `dt-*` helpers apply; `prose` (default true) adds `.gummy-html-prose` from the new portable partial `styles/htmlContent.css` (headings, paragraphs, lists, quotes, code, tables, `mark`, images — Radix vars only), `prose: false` for markup that brings its own layout. Demo in `apps/example`: the **HTML content** page (`/html-content`, `config/country/htmlContentDemo.ts`, menu icon `code`) — a static CMS-style block beside a `prose: false` one, one block per region in a repeater (`{{name}}`, `{{about}}`, `{{countries.length}}`, a routed link from `{{countries.0._id}}`), an `html` bin as a card's content, and what the sanitiser strips. Studio: `html` tile (Display group), `HtmlContentConfig { name, html, binding: ItemBinding, prose }`, `Layout/HtmlContentConfigPanel.tsx` — the data table's `ColumnHtmlField` reused (it now takes a `Pick<…, 'html' | 'accessor'>`, a structural `PickableFields` for "Insert field", and `rows` / `previewLabel` / `prose` props) over the enclosing repeater's item fields, an `ItemBindingField` for what the placeholders read (the item itself, `key: 'none'`, by default) and a prose toggle; `htmlContentElement` in `gridConfig.ts`; `HtmlContentLivePreview` on the canvas renders the real `HtmlCell` over `buildHtmlPreviewValue(template)` (`dataTablePreview.ts`: every `{{path}}` shows its `{path}` token, `.length` a count); the studio `index.css` imports the `htmlContent.css` partial; workspace `version: 36` (`migrateV35`: the seeded **HTML content** page `seed-page-html-content`, `htmlContentDemoSeedItems` / `htmlContentDemoPage`).
- **Drawer** (`drawer` Bin, `components/Drawer.tsx` + `Drawer.css`, builder `core/drawer.ts`; grilled design after MUI's *temporary* Drawer, see memory `drawer-component`): a panel sliding in over the page from `anchor: left | right | top | bottom` (default `right`) behind a scrim — `DrawerElement { id, title?, description?, container, trigger: PopoverTrigger, anchor?, size?, hideHeader? }`. It is the modal's shell (Radix Dialog: portal, scrim, focus trap, Esc, `fnCtxs[id]` registration, the inner `ThemeProvider` that carries the Radix panel vars into the portal — the `.modal-overlay` scrim and `.modal__action` close button are reused) with the content pinned to one edge and slid in along that axis (`.drawer-content[data-anchor]` keyframes in `Drawer.css`, imported by `styles.css` and the studio `index.css`); `size` is the width of a left / right drawer or the height of a top / bottom one (any CSS length; defaults `360px` / `50vh`, `DEFAULT_DRAWER_SIZE`, never past the viewport), `hideHeader` drops the title row (an sr-only title keeps the dialog named). The `container` mounts its **own** form like `modal` / `popover` (`ContainerBuilder.draw`; `schema.ts` ignores it), so a form inside can Save through its own API and a `CloseModal` button with `modalId` = the drawer's `id` closes it. The trigger is the popover's mini-Bin (`{ type, element }` through the element registry): a `button` is handed to Radix's `Trigger asChild` directly, any other element (card, avatar, text) is wrapped in a `display: contents` span the click bubbles to, since only `Button` forwards the ref / onClick. Two engine fixes came with it (`Button.tsx`): **`OpenModal`** is implemented — `fnCtxs[modalId](true)`, so any button opens a modal or drawer by id (warns without `modalId`); and a **failed validation now ends the action chain** (before, `StopLoading` / `CloseModal` after a `SubmitFormTo*` still ran on an invalid form and closed the modal over the user's input). `IconData` gained `panelRight` / `panelRightClose` / `panelRightOpen` / `panelTop` / `panelBottom`. The persistent / permanent variants are app chrome (the `AppShell` sidebar) and deliberately not a Bin. Demo in `apps/example`: the **Drawer** page (`/drawer`, `config/country/drawerDemo.ts`, menu icon `panelRight`) — the four anchors (MUI's mail list), a form drawer posting to `createContact` with Cancel / Save, a header-less 240px nav drawer, a card as the trigger, and an `OpenModal` button opening a bottom sheet by id. Studio: `drawer` tile (Overlay group, `PanelRight`), `DrawerConfig { id, title, description, anchor, size, hideHeader, trigger: ButtonConfig }` (`createDefaultDrawerConfig`, `drawer_${n}` ids), one child canvas (`childCanvasCount` 1, drill-in via `EditContentsButton`), `DrawerConfigPanel` in `ContainerHostConfigPanel.tsx` (Anchor segmented control, Width / Height field with the default as placeholder, Hide header toggle, trigger label / icon / variant), `drawerElement` in `gridConfig.ts` (button trigger emitted as the mini-Bin, `anchor` / `hideHeader` / `size` only off the defaults), the canvas renders the trigger only (`DrawerLivePreview`, always-live tier — the panel is exercised in the Live Preview), `collectButtonTargets` lists drawers beside modals so a `CloseModal` button can pick one, Style tab parity with modal; workspace `version: 37` (`migrateV36`: the seeded **Drawer** page `seed-page-drawer`, `drawerDemoSeedItems` / `drawerDemoPage` — the anchors, the form drawer and the nav drawer; the card trigger and `OpenModal` demos aren't seedable, the studio authors button triggers and has no `OpenModal` action yet).
- **Switch** (`switch` Bin, `components/Switch.tsx` → `SwitchBase`, form-bound as `form/Switch.tsx` via `withForm`, builder `core/switch.ts`; grilled design after MUI's Switch, see memory `switch-component`): one boolean field drawn as Radix Themes' `Switch` — `SwitchElement { name, dataType: "boolean", isRequired, errorMessage, label?, labelPosition?: "start" | "end" (default end), helperText?, defaultChecked?, disabled?, size? 1|2|3 (default 2), variant? surface|classic|soft, canObserve?, enabledWhen?: CondExpression, value?: DataValue }`. **`isRequired` means it must be on**: `schema.ts` now maps a required `boolean` to `z.literal(true)` (this also makes a required single checkbox mean "checked"); the builder seeds the form value from `defaultChecked` through `withForm`'s `defaultValue`, so an untouched switch validates as `false`, not `undefined`. The label is a real `<label htmlFor>` (Radix's switch is a button), so clicking it toggles; the error state paints only on the form's verdict (`error` from `withForm`), never from a configured `errorMessage` alone. **Gating**: `canObserve: true` publishes the value on the engine's observe Subject keyed by `name` (and re-publishes the current value once the Subject exists — Subjects don't replay, and the gated switch may mount first); `enabledWhen: { left: { key: "<gate name>", type: "observe" }, operator: "eq", right: { val: true } }` keeps the switch enabled only while the condition holds, disabled until the first value arrives (fails closed). Side fix: `Autocomplete2` / `MultiAutocomplete` / `RadioButton2` negated their `enabledWhen` result (it read as "disabled when") — nothing authored it, now it means enabled-when everywhere. `IconData` gained `toggleLeft` / `toggleRight`. Demo in `apps/example`: the **Switch** page (`/switch`, `config/country/switchDemo.ts`) — basics, label position, sizes / variants, disabled + a gate pair, and a required "Accept the terms" in front of a Save with no `api` (`SubmitFormToPostAPI` then only validates and reports). Studio: `switch` tile (Inputs group, `ToggleLeft`), `SwitchConfig { name, label, labelPosition, helperText, isRequired, errorMessage, variant, size, defaultChecked, disabled, enabledWhen: { itemId, when: 'on' | 'off' } | null }` (`createDefaultSwitchConfig`), `Layout/SwitchConfigPanel.tsx` (an **Enabled when** picker over the other switches of the active canvas + "is on / is off", with dangling / unnamed / mutual-gate warnings — only switches gate, the engine checkbox doesn't publish), `switchElement` in `gridConfig.ts` (the gate is stored by item id and resolved to the name-keyed pair at export: `enabledWhen` on the gated item, `canObserve: true` on the gate, `MISSING_OBSERVE_TARGET` when broken; look keys only off the defaults), `SwitchLivePreview` on the canvas (always-live tier; a gated switch previews disabled), blocked inside a repeater like the other form-bound types; workspace `version: 39` (`migrateV38`: the seeded **Switch** page `seed-page-switch`, `switchDemoSeedItems` / `switchDemoPage`).
- **DataTable header gap**: `DataTableElement.headerGap?: "0" | "1" | "2" | "3" | "4" | "6" | "8"` (default `"2"`) spaces the header's title, Add button and search box (a literal `gap-*` map in `DataTable2.tsx`, applied to the header row and its controls group). Studio: `DataTableConfig.headerGap` (`''` = default) as a "Header gap" select, exported only when set; workspace `version: 24` (`migrateV23` fills `''`).
- **DataTable search box**: `DataTableElement.canSearch?: boolean` (default true) shows/hides the header's "Search all columns" box (`DataTableProps.canSearchAllColumns`; in server paging it also needs `api.pagination.searchKey`). Studio: `DataTableConfig.canSearchAllColumns` ("Search box" toggle) exports `canSearch: false` when off. The example's Pagination page hides it on the in-memory table.
- **DataTable Add action** (grilled design, see memory `datatable-add-action`): `DataTableElement.canAdd` + `addButton?: { label, icon, variant }` (defaults "Add" / `puls` / contained; empty label ⇒ icon-only) render an Add button in the table header before the search box. It writes `contextData[name] = {}` and opens the table's **own** modal — the same `modalContainer` as Edit — so one form serves both modes; the form tells them apart with a Bin `condition` on `<name>._id` (`eq undefined` ⇒ adding), exactly as the example's country form does with its Create (POST) / Update (PATCH) buttons; the form's `CloseModal` closes it through the `modalEdit` fn ctx. Studio: `DataTableConfig.canAdd` + `addButton: ButtonConfig` under an "Add action" toggle (`DEFAULT_ADD_BUTTON`; exported only off the defaults), the edit canvas is emitted for either action, and items inside a data table's modal canvas get a **Show when** control (`GridItemData.showWhen: 'always' | 'adding' | 'editing'`, `Layout/ShowWhenPanel.tsx` rendered by the Sidebar when `enclosingDataTable(items, activePath)` finds one; `gridStore.updateItemShowWhen`; `buildBins` takes a `CanvasScope { tableName }` from `dataTableElement` and exports the engine `condition`). Workspace `version: 23` (`migrateV22`: defaults on every table; the seeded Countries table gets `canAdd` + "Add Country", its canvas gains the Create button (`countryCreateButtonSeedItem`, adding-only), Update becomes editing-only and the standalone `seed-item-country-modal` is removed — all guarded on the seed ids). The example's Countries page likewise drops its standalone modal bin for `canAdd` on `dtCountry`.
- **File upload** (`uploadfile` Bin, `components/UploadFile.tsx` + `UploadFileParts.tsx` + `UploadFileViewer.tsx`, builder `core/uploadFile.ts`; grilled design, see memory `upload-file-preview-accept`): the general uploader — `uploadimage` stays the single hero-image picker. The value is **always** `UploadedFile[]`; `multiple` (default false) only switches the UX: single mode swaps the dropzone for the file itself (Replace / Remove; a replace in `api` mode best-effort DELETEs the old file). `accept: AcceptConfig` is a comma list or an array mixing presets (`image|pdf|document|spreadsheet|presentation|text|archive|audio|video`, one map in `util/accept.ts`) with raw tokens (`.dwg`, `image/png`); `resolveAccept` feeds both the `<input accept>` and the JS check on pick **and** drop (`matchesAccept`, extension fallback when the browser gives no mime), `acceptLabel` the dropzone hint. Batches are partial: files failing type / `maxSizeMB` / `maxFiles` / the upload itself (`Promise.allSettled`, one POST per file) are skipped and listed in the error line, the rest land. `preview` (default true) + `previewLayout: list|grid` draw image thumbnails / kind icons (`fileKind` in `util/file.ts`) and open `UploadFileViewer` — the library `Modal`, controlled: image / PDF iframe / video / audio / text `<pre>`, prev/next + arrow keys, Open in new tab + Download, usable while disabled. The viewer builds **blob URLs with a forced mime** (text → `text/plain`, unknown → octet-stream) and never opens an SVG as a document, since a blob shares the page origin; the mock API likewise serves `.svg`/`.html` as octet-stream. Surfaces use Radix vars. Tests: `util/accept.test.ts`. Studio side: `UploadFileConfig` gains `acceptPresets` (chips) beside the free-text `accept` ("Extra types"), merged by `uploadAccept` for export and the canvas, plus `preview`/`previewLayout` (exported only off the engine defaults) and a Single | Multiple segmented control over `multiple`; workspace `version: 17` (`migrateV16`); the live cell is fed `UPLOAD_SAMPLE_FILES` (canvas-only) so list/grid and the single-mode card are visible. Demos: the example app's **Upload** page (`/file-upload` — *not* `/upload`, which Vite proxies to the API; `config/country/uploadDemo.ts`: mode, accept and preview sections, one config per field) and the studio's seeded **Upload** page (`seed-page-upload`, `uploadDemoSeedItems` / `uploadDemoPage`; workspace `version: 18`, `migrateV17` via `appendDemoPages`).
- **Imperatively-used components** also exist (DataTable2, DataTableEditable, Autocomplete2, Modal, Snackbar, etc.) and are exported from `src/index.ts` for direct JSX use alongside the declarative engine.

The public surface is curated in `packages/ui/src/index.ts` — add new exports there.

## Studio live cell previews (size-gated chip ↔ live component)

`apps/studio` (the visual layout builder) renders the **real library component** inside a grid cell once the cell is big enough, and a compact **chip** when it isn't. When adding a live preview for another component type (Select, DatePicker, …), follow this recipe — it's implemented for `textfield` in `apps/studio/src/components/Layout/GridItem.tsx`:

1. **Gate on measured cell width, not breakpoint.** `useIsLiveWidth(el)` decides chip vs live from the cell's content-box width against a per-kind threshold (`thresholdsForType`: ~14rem for fields, wider for tables, and an `ALWAYS_LIVE` zero-threshold tier for display/trigger types — divider/text/typography/avatar/button/modal/popover) with a **hysteresis band** (flip to live at `liveMin`, back to chip only below `chipMax`) so a cell resting on the boundary doesn't strobe between subtrees. `hidden` never goes live (it renders nothing at runtime).
2. **Measure synchronously, then observe.** Read the width in a `useLayoutEffect` (`clientWidth` − horizontal padding) so the correct state is set *before paint*, then attach a `ResizeObserver` for later resizes. Do **not** rely on the observer's async first callback alone — it flashes a chip on mount and never fires in throttled contexts (hidden/background tabs; see [[studio-preview-hidden-tab]]). The measured element is held in `useState` (callback ref) and merged with dnd-kit's `setNodeRef`; the decision stays local to `GridItem` — nothing goes in `gridStore`.
3. **One generic seam.** `renderLive(item, isLive)` returns the real component or `null`; each new type is one branch there. Anything without a live render falls through to its chip even when wide.
4. **Inert preview.** Wrap the live component in `pointer-events-none` so a click still selects the cell and drag still works via the grip — you preview the component, you don't interact with it on the canvas.
5. **Let the cell own sizing.** Map the component's editable config 1:1 onto its props, but force `isFullWidth` and drop `isFixedHeight` in-cell so the field fills the cell width and the grid row controls height. Props with no equivalent (e.g. a required marker) get baked into an existing prop (the label).
6. **Theme comes free.** Studio wraps the canvas in `ThemeProvider` (`App.tsx`); real library components inherit the accent/appearance — no extra wiring. (Studio consumes the library's built `dist/`, so rebuild the library after changing component source — see the studio toolchain notes.)

### Studio workspace and shared library

Studio is a two-level app persisted to one localStorage key (`gummy.studio.workspace.v1`, `version: 2`) by `components/Workspace/workspaceStore.ts` (zustand `persist`; `merge` migrates v1 payloads). The **portal** (`/`, `components/Workspace/Portal.tsx`) lists projects and hosts the **shared library** pages (`/library/apis`, `/library/models` → `components/Library/LibraryPage.tsx`). A **project** (`/p/:id/*`, `ProjectShell.tsx`) owns only its canvas, env and theme plus `endpointIds` attached from the library; its models are derived from those endpoints' refs. The five live zustand stores are unchanged editors: `ProjectShell` hydrates grid/env/theme from the project snapshot and autosaves them back; `Library/LibrarySync.tsx` hydrates the group/model/api stores from `workspace.library` once at boot and autosaves them back. Scoping is a React context (`Library/scope.ts`: `useScopedEndpoints/useScopedModels` for the editors, `useProjectEndpoints/useProjectModels` for grid inspectors, code export and Live Preview) — never filter by reaching into the workspace store from a component. `groupId` on `EndpointDef`/`ModelDef` is editor-only and never serialized.

### Studio chrome styling (tokens, not palette colours)

The studio's own chrome follows the `gummy-studio` mockup: a monochrome "ink" token set defined in `apps/studio/src/index.css` (`:root` light, `.dark` dark) and exposed to Tailwind via `@theme inline` — use `bg-surface / bg-panel / bg-panel-2 / bg-sunken`, `text-ink / text-ink-2 / text-ink-3`, `border-line / border-line-strong`, `bg-accent text-accent-ink`, `ring-focus`, and `text-danger / text-warn / text-ok`. Never `zinc-*`/`teal-*` in studio chrome — one class must work in both appearances (the library ThemeProvider toggles `.dark` on `<html>` from the Theme page's `appearance`, so chrome and canvas flip together). Compact type uses `text-ui` (12.5px) / `text-ui-sm` / `text-ui-xs`, leaving `text-sm` etc. to the previewed library components. Shared primitives (`btn`, `field`, `seg`/`seg-btn`, `tag`, `mini-tab`, `list-row`, `sec-label`, `box`) are `@utility` classes in the same file, wrapped by `components/common` (`Button`, `IconButton`, `Input`, `Select`, `SegmentedControl`).

### Nested canvases (container / paper / tab / modal / popover)

Container-hosting types carry **child canvases** (`GridItemData.childCanvases`: one for container/paper/modal/popover, one per tab header, arbitrary depth). Editing is **drill-in**: `enterCanvas`/`exitToDepth` in `gridStore` track an `activePath`, only one flat canvas is ever active (dnd-kit/selection/inspector unchanged), and a breadcrumb in `PreviewToolbar` navigates back. All store mutations route through `setActiveCanvas` (immutable spine rebuild via `updateCanvasAtPath`). Parent cells render children **read-only** via `ChildCanvasPreview` (recursive, one `pointer-events-none` at the top); modal/popover cells render only their trigger — their content is exercised in the Live Preview modal, where the real engine can open the overlay. Serialize recurses through `toEngineContainer` (`gridConfig.ts`); the tab panel's add/remove goes through `addTab`/`removeTab` so `TabConfig.tabs` and `childCanvases` stay index-aligned.

### Studio pages, templates, activity and design-only types (from the `gummy-studio-20260909` mockup)

- **Pages.** A project snapshot is `{ pages: PageDef[], env, theme, endpointIds }` (workspace `version: 4`; v1/v2/v3 payloads are migrated in `workspaceStore.ts` `merge` — v3→v4 derives page keys and turns the old design-only "go to page" into the engine `Navigate` action). Each page is `{ id, key, name, path, grid }` where `path` is the exported route (`/countries/:code`; `pathParams` in `Workspace/snapshots.ts`) and `key` is its identifier in the exported `pages` record (`pageKey`/`uniquePageKey`, editable in `PageDialog`, unique per project; navigation refs store the `id` and resolve to the key at export). `gridStore` stays a single-canvas editor: the Layout tab lives at `/p/:projectId/pages/:pageId` and `ProjectShell` hydrates only that page, flushing the outgoing page's autosave on a switch. Page CRUD (`addPage/updatePage/movePage/deletePage`) is on the workspace store; the canvas bar (`Layout/PageBar.tsx`) and the left pane's Pages tab (`Layout/PagesPanel.tsx`, a tree by button links via `Layout/pageLinks.ts`) drive it.
- **Undo/redo** is a per-page snapshot history recorded by a `useGridStore.subscribe` in `gridStore.ts` (cap 50, consecutive same-structure edits within 600 ms coalesce, cleared on `hydrate`). `useUndoShortcuts` binds Cmd/Ctrl+Z and Shift+Cmd+Z outside text controls.
- **Design-only component types** (`Layout/designTypes.ts`: hero, image, gallery, video, linkcard, quicklinks, spacer, bar/line/pie chart, people, orgchart, stat, alert, banner, progress, toast, dialog) have no `@gummy-ui/ui` counterpart. They render their design on the canvas (`Layout/DesignPreviews.tsx` — the seam a real component slots into later), have Props panels (`DesignConfigPanel.tsx`), export as an `empty` bin carrying `designOnly: { type, label, config }`, and the Live Preview turns them into a labelled placeholder (`livePreview.ts`). Palette tiles mark them with a dot (`designOnly: true` in `componentCatalog.ts`).
- **Style tab** (`Layout/StylePanel.tsx`) writes `GridItemData.style` (`ElementStyle`: bg/text/border/accent/radius, table header colours, zebra) — a design-only annotation painted through CSS variables on the cell (`elementStyleVars`) and exported on the bin as `style`; containers derive the engine `surface` background/border flags from it.
- **Page navigation is engine-backed** (see the page-router design in `packages/ui`): a button's `Navigate` action carries `ButtonItemConfig.navigate` and a data table's row click `DataTableConfig.rowNavigate`, both a `StudioNavigate` (`pageId` + one `NavParamSource` per `:param` — literal / URL param or query / global state / clicked row — + `replace`). Export resolves `pageId` → `PageDef.key` (loud `MISSING_PAGE` when deleted) into the engine `NavigateTarget` on the element, and `pages.ts` emits the keyed `TPageMaster` record `PageRouter` takes as is. `NavigateEditor` (`ButtonConfigPanel.tsx`) is the shared editor. The remaining `DesignNavigation` kinds (link, toast, dialog) stay design-only: exported on the bin as `designNavigation` and played by `LivePreviewModal` with a capture-phase click matched by button label; click-only toasts/dialogs (`trigger: 'button'`) are filtered out of the page.
- **App-shell menu** (grilled design, see memory `config-app-shell-menu`): the snapshot carries `menu: MenuItemDef[]` (workspace `version: 5`; v4 payloads get `defaultMenu` — one link per param-less page) and, since `version: 6`, `shell: { navigation: 'sidebar' | 'top', breadcrumbs }` since `version: 7` `shell.appBar: { title, icon, logo, variant }` since `version: 8` `shell.menuIcons` (`migrateV7`, a checkbox in the Layout section) and since `version: 9` `shell.collapsible` + `shell.appBar.sidebarToggle: { hide, show }` (`migrateV8`; a checkbox and two icon fields in the App bar block, the fields disabled while not collapsible) (`defaultShell(title)` / `defaultAppBar`, title defaulting to the project name — `migrateV6`; edited as the Layout and App bar sections at the top of the Menu tab; `shellToEngine` in `Workspace/menu.ts` turns it into the `AppShell` props — empty app-bar fields dropped — for both the `export const shell` in `menu.ts`, spread onto `<AppShell>` in the HANDOFF wiring, and the Live Preview). Breadcrumbs are authored per page in `PageDialog`: `PageDef.parentId` (cycle-guarded in `updatePage`), `crumb?: NavParamSource` (literal / URL param / query only — no `state` until a container `load` is authorable) and `hideBreadcrumbs`; `Workspace/pageMeta.ts` (`enginePageMeta`) turns them into the `PageElement` `title`/`parent`/`breadcrumb` for both `pages.ts` and the Live Preview, which passes `documentTitle={false}` to `PageRouter` so the studio tab keeps its name. `Menu/MenuEditor.tsx` at `/p/:id/menu` edits it (sortable cards, groups one level deep, `NavigateEditor` for page links, an Icon field per item — glyph + key trigger opening the shared `IconPicker` in the studio `Popover`, glyph echoed in the card header); `Workspace/menu.ts` converts it to the library `TMenu` (`menuToEngine`, page ids → keys via `toNavigateTarget`) for the `menu.ts` export and for the Live Preview, which now renders `PageRouter` inside the real `AppShell` (`height` bounded to the modal).
- **Live Preview routes for real**: `LivePreviewModal` renders every page's exported container through the library `PageRouter` inside a `MemoryRouter`, wrapped in `NestedRouterBoundary` (resets react-router's `UNSAFE_*` contexts so a router can nest inside the studio's `BrowserRouter`). The page bar shows the memory location, one input per `:param` of the matched page (a page opened directly has its params unfilled), a page select and history back. `apps/studio/vite.config.ts` aliases `react-router-dom` to the studio's v7 copy so the library's built output (peer dep, otherwise the root v6) shares the studio's router contexts.
- **Templates** live in `workspace.library.templates` (`TemplateDef`, seeded by `seed/templates.ts`). Portal page `/library/templates` (`Library/TemplatesPage.tsx`), master-layout editor `/library/templates/:id/layout` (`Library/TemplateShell.tsx`, `Layout templateMode`), studio Templates tab (`Layout/TemplatesPanel.tsx`) inserting via `cloneItems` (fresh ids, remapped refs, page navigation dropped) and `gridStore.appendItems`. `saveLibrary` merges so the live-store sync never drops templates.
- **Activity** (`workspace.activity`, cap 500) is written by `logActivity` from the coarse store actions and by `Library/LibrarySync.tsx` diffing the library stores (created/renamed/deleted). Identity is `workspace.user` from `MOCK_USERS` (`Workspace/UserButton.tsx`). Page: `/activity` (`Workspace/ActivityPage.tsx`).
- **Overview / Export** (`Workspace/OverviewDialog.tsx`, `ExportDialog.tsx` + `exportFiles.ts`) open from the topbar; export bundles project.json, api.ts, model.ts, theme.ts, routes.ts, HANDOFF.md.
- **Theme extras**: palette presets snap to the nearest Radix accent (`nearestAccent` in `Theme/types.ts`), density → Radix `scaling` (exported in theme.ts; note the library `ThemeProvider` does not yet forward `scaling` to `<Theme>`), per-mode surfaces + font are `StudioThemeConfig.design` (design-only, painted on the canvas frame and Live Preview wrapper via `Theme/designTheme.ts`).
- **Form list** (studio side): catalog tile in Data, `FormListConfig` (`types.ts`: labels, `readPaths`, three action toggles, four `FormListCrudRef`s — `endpointId` + `params: Record<:param, NavParamSource>` seeded from the endpoint URL on pick (a `row` source → engine row-field `params`, any other → `extraParams` / read `params`) + free-form `extra` (read `query` / mutation `extraBody`) — the Add / Remove button label, icon (`IconField`, `''` = default glyph), position and display (`both|icon|label`; workspace `version: 15`, `migrateV13` fills the defaults), and the delete confirm fields), one child canvas = the row template (`childCanvasCount`), `FormListConfigPanel.tsx` (reuses `EndpointPicker`, `EditContentsButton` and `ParamSourceFields` — the per-param source controls factored out of `NavigateEditor`), `formListElement` in `gridConfig.ts`, `FormListLivePreview` on the canvas (chrome + row template; rows are exercised in the Live Preview, whose wiring check reads `apiCrud.read` and recurses into `rowContainer`). The seeded Country manager's detail page carries a **Languages** list (`countryLanguagesSeedItem`) over the `languages` seed models/endpoints (workspace `version: 12`, `migrateV11`), and a **Form list** page (`seed-page-form-list`, `formListDemoSeedItems` / `formListDemoPage`: the contacts + notes demo over the `contacts`/`notes` seed models/endpoints; workspace `version: 13`, `migrateV12` — both migrations go through `seedLibraryAdditions` + `appendDemoPages`).
- **Repeater** (studio side): catalog tile in Data, `RepeaterConfig` (`types.ts`: `source: 'endpoint' | 'parent'`, `read: FormListCrudRef` + `readPaths`, `parentField`, `itemSpan: Responsive<number>` out of 12, `gap`, `itemSurface`, `itemPadding`, `itemNavigate: StudioNavigate | null`), one child canvas = the item template, `RepeaterConfigPanel.tsx` (reuses the form list's exported `CrudRefEditor`, `EditContentsButton`, `NavigateEditor` with `allowRow`; the **Parent item field** source is offered only inside another repeater's template — `state` stays engine-only until container `load` is authorable), `repeaterElement` in `gridConfig.ts` (spans export like a bin's; `row` param sources become DataValues reading the *enclosing* item), `RepeaterLivePreview` on the canvas (template + faded ghost copies in the real span grid at the previewed breakpoint; always-live tier). Item bindings: `TextConfig.binding` / `TypographyConfig.binding` / `AvatarConfig.srcBinding` + `fallbackBinding` (`ItemBinding = { key, path } | null`) edited by `ItemBindingField.tsx` — a `Static | Item field` switch shown only inside a repeater's template, `FieldPicker` over the enclosing repeater's item model (`repeaterScope.ts`: ancestors read off `activePath`, endpoint source → `rowModelFields` in `rowFields.ts`, parent source → the children of the parent item's array-of-objects field, recursively; `none` = the item itself) plus an optional `path`; the canvas shows `{field}` tokens (`bindingToken`), a button's `NavigateEditor` gains the row source there too. `repeaterRules.ts` (pure, no store import) blocks the form-bound types inside a repeater's template: `gridStore.addItem` refuses them, `appendItems` strips them from inserted templates, the palette tiles disable with a hint; `cloneItems` drops `itemNavigate`. The Live Preview's wiring check reads `api.name` and recurses into `itemContainer`. Workspace `version: 16` (`migrateV15`: binding nulls on every canvas, the `regions` seed model/endpoint via `seedLibraryAdditions`, and the seeded **Repeater** page `seed-page-repeater` — `repeaterDemoSeedItems` / `repeaterDemoPage` — via `appendDemoPages`).
- **Select-family option display** (grilled design, see memory `select-option-display`): `select`, `autocomplete` and `multiAutocomplete` share `SelectFieldConfig`, whose option keys are **one flat set for both modes** — `idKey/displayKey/searchKey` plus `subtitleKey`, `avatarKey`, `inputIcon`, `itemIcon` (`''` = unset; workspace `version: 10`, `migrateV9` carries the old source-mode `dataSource.valueKey/labelKey` — edited but never exported — onto `idKey`/`displayKey`+`searchKey`). The panel's "Option display" section picks them with the common `FieldPicker`, fed by `Layout/rowFields.ts` `rowFieldsFor` (source mode: the endpoint's response `ModelDef` walked along `dataSource.paths` to the array-of-object row; static mode: the union of record keys; unresolvable → free-text fallback with a reason; a stale key stays selected behind a ⚠; top-level fields only because the engine reads `item[key]`), two `IconField`s (now in `components/common` with `IconPicker`) and an "Option preview" strip rendering the library's `OptionRow`. Export (`autocompleteElement` in `gridConfig.ts`) emits `keys`, `itemSubtitle`/`itemAvatar`/`itemIcon`/`inputIcon`/`placeholder`/`helperText` only when set, `options: []` in source mode, and `maxSelections`/`showSelectedCount` for the multi. In the library, `OptionRow.tsx` holds the row markup + resolvers + `matchesOptionQuery` (search key OR subtitle field) and `OptionSearch.tsx` the dropdown's search field (inset pill on a padded header, accent focus ring, spinner/clear slot, match count; `OPTION_SEARCH_HEIGHT` feeds the dropdown positioning) shared by `Autocomplete2` and `MultiAutocomplete`; the single trigger shows the selected item's avatar in place of `inputIcon`; the multi's **selected chips** are `SelectedChip` (same file, exported): the row's leading visual — `itemAvatar` over `itemIcon`, a 16px `Avatar` / 12px icon — plus the title and a labelled ✕ (`aria-label="Remove <title>"`), automatically whenever the rows have one (no key, no toggle; the subtitle stays on the row), and the studio's Option preview strip shows the first option as a chip too for `multiAutocomplete` (the `multi` flag threads through `OptionDisplaySection`); the seeded **Chips** page (`seed-page-chips`, `chipsDemoSeedItems` / `chipsDemoPage`, workspace `version: 38`, `migrateV37` via `appendDemoPages`) puts three multis side by side — flag chips (source mode + Image field), icon chips (static + Option icon) and plain title chips. The demo project seeds an **Option display** page (`seed-page-option-display`, `optionDisplaySeedItems` in `seed/country.ts`) with a static Select and a source-mode Autocomplete + Multi Autocomplete already configured, and an **Icons** page (`seed-page-icons`, `iconDemoSeedItems`) showing the option icon and input icon slots on their own — both defined once in `optionDemoPages()` (`Workspace/snapshots.ts`) and appended to an existing seeded project by `migrateV10` (workspace `version: 11`; page ids absent → page + sidebar link added, so a deleted page stays deleted); a multi always exports `dataType: "array"` (its form value is the id list).
- Engine contract untouched on purpose: breakpoints stay `sm/md/lg/xl` out of 12 columns, engine bins keep their shape; everything mockup-only rides beside them under `designOnly` / `designNavigation` / `style` keys.

## Conventions

- Functional components + hooks; Tailwind for styling (no inline styles); Radix UI primitives for accessibility.
- PascalCase component files, camelCase utilities. Type all props; use Zod/TypeBox for runtime validation where the engine expects it.
- Match the config shapes (`TModelMaster`, `TApiMaster`, `Bin`/`Container`) exactly — they are the contract the builders rely on.

## Theming rule (apply when creating or editing any component)

Every component you add or change **must follow the theme** in two ways. Don't hardcode colors:

1. **Light/dark mode.** Surfaces, text, and borders flip with the `dark` class on `<html>` (Tailwind `darkMode: 'class'`). Use `dark:` variant classes (`bg-white dark:bg-gray-900`, `text-slate-800 dark:text-gray-100`, `border-slate-200 dark:border-gray-700`) — never a bare `#fff`/hex/static `bg-white`. Inline styles are only acceptable for genuinely dynamic values that can't be a class (e.g. a computed elevation shadow); everything else goes through classes so `dark:` resolves.
2. **Accent (primary) color.** For brand/primary surfaces follow the Radix accent via the `--accent-*` CSS vars (`bg-[var(--accent-9)]`, `text-[var(--accent-contrast)]`, `hover:bg-[var(--accent-10)]`, `ring-[var(--accent-6)]`). These re-tint automatically for dark appearance and track the configured `accentColor` — prefer them over named Tailwind palette colors. Read per-component overrides from `useTheme()` (`theme.components.*`) and fall back to the accent vars when unset.

Two constraints that make this work:
- **Tailwind only emits classes that appear as literal strings** in scanned source (`packages/ui/src/**`). Runtime-built class names are dropped — keep accent/dark classes as literal constants or static lookup maps (see `tableBgColors` and the `ACCENT_*` constants in `src/util/constant/colors.ts`).
- **`@apply dark:…` compiles to the portal-safe descendant form** `.sel:is(.dark *){…}`, which covers Radix-portaled content (modals/popovers at body level). Use it in `src/styles.css` rather than `.dark .sel {}` so portaled chrome flips too.
